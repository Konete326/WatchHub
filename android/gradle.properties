org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Disable automatic SDK component downloads to prevent NDK installation when disk space is limited
# Temporarily enabled to allow NDK re-download after malformed installation
android.builder.sdk.disableAutoDownload=false
# Additional properties to prevent NDK auto-download
android.ndkVersion=
# Prevent SDK manager from auto-installing components
android.sdk.installMissingComponents=true
